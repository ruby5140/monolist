<% if current_user.want?(item) %>
  <%= form_tag(ownership_path(item.id), method: :delete) do %>
    <%= hidden_field_tag :item_id, item.id, id: "want_item_id_#{item.code}" %>
    <%= hidden_field_tag :type, 'Want', id: "want_#{item.code}" %>
    <%= submit_tag 'Want', class: 'btn btn-success' %>
  <% end %>
<% else %>
  <%= form_tag(ownerships_path) do %>
    <%= hidden_field_tag :item_code, item.code, id: "want_item_id_#{item.code}" %>
    <%= hidden_field_tag :type, 'Want', id: "want_#{item.code}" %>
    <%= submit_tag 'Want it', class: 'btn btn-primary' %>
  <% end %>
<% end %>